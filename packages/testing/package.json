{
  "name": "@complianx/testing",
  "version": "0.1.0",
  "private": true,
  "description": "Shared test builders and fixtures",
  "scripts": { "build": "echo 'Testing package is source-only'", "test": "echo 'No shared tests yet'", "lint": "echo 'No testing lint yet'", "format": "echo 'No testing format yet'" }
}
