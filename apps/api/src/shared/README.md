# Shared API code

Only generic, domain-neutral errors, decorators, guards, validation, pagination, types, and utilities belong here. Shared code must not depend on domains or platform implementations.
