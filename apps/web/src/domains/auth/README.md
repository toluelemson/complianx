# Auth

Target home for sign-in, sign-up, verification, password recovery, and user-session presentation. Technical token attachment belongs in `platform/api`; provider composition belongs in `app/providers`.
